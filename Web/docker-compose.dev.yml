services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run dev
    environment:
      - NODE_ENV=development
    volumes:
      - ./src:/app/src
      - ./public:/app/public
