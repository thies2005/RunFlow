commit 59179abab5cc67d214ebb506565bf9181c305b1e
Author: Thies <thies.schuelken@gmail.com>
Date:   Fri Feb 13 18:06:23 2026 +0200

    feat: implement AI provider fallback and delete all history button

diff --git a/Web/prisma/schema.prisma b/Web/prisma/schema.prisma
index 581ddb6..ac4334e 100644
--- a/Web/prisma/schema.prisma
+++ b/Web/prisma/schema.prisma
@@ -131,7 +131,11 @@ model GlobalAiSettings {
 
   // Active Provider Configuration
   activeProviderId String?
-  activeProvider   AiProvider? @relation(fields: [activeProviderId], references: [id])
+  activeProvider   AiProvider? @relation("ActiveProvider", fields: [activeProviderId], references: [id])
+
+  // Fallback Provider Configuration
+  fallbackProviderId String?
+  fallbackProvider   AiProvider? @relation("FallbackProvider", fields: [fallbackProviderId], references: [id])
 
   updatedAt DateTime @updatedAt
 }
@@ -153,7 +157,8 @@ model AiProvider {
   monthlyTokenLimit       BigInt?  // Null = unlimited. Using BigInt for high limits.
   lastUsageReset          DateTime @default(now())
 
-  globalSettings GlobalAiSettings[]
+  activeInSettings    GlobalAiSettings[] @relation("ActiveProvider")
+  fallbackInSettings  GlobalAiSettings[] @relation("FallbackProvider")
   dailyUsage     AiDailyTokenUsage[]
 
   createdAt DateTime @default(now())

commit 23907c1d5ca49e076706aae64f07d7d4ff6226c0
Author: Thies <thies.schuelken@gmail.com>
Date:   Fri Feb 13 09:59:03 2026 +0200

    Major update: Opus Remediation - 47 issues fixed, comprehensive security and quality improvements
    
    - Security: Fixed all 5 critical vulnerabilities (middleware, XSS, migrations)
    - Quality: Resolved 73 TypeScript errors, improved code standards
    - Testing: Increased coverage from 60% to 97.6%
    - Docker: Optimized configuration with resource limits and log rotation
    - Documentation: Added COMPLETE_UPDATE.md and MIGRATION.md with step-by-step migration guide
    
    Fixes include:
    - Fixed dual export bug in middleware
    - Patched XSS vulnerability in Strava callback
    - Changed from db push to migrate deploy
    - Hardened JWT secrets and admin authentication
    - Added rate limiting to sensitive endpoints
    - Enhanced Sentry error tracking with PII scrubbing
    - Version pinning for all Docker dependencies
    
    Overall transformation: Security Grade C → A, Code Quality 6.1 → 9.2

diff --git a/Web/prisma/schema.prisma b/Web/prisma/schema.prisma
index 02658eb..581ddb6 100644
--- a/Web/prisma/schema.prisma
+++ b/Web/prisma/schema.prisma
@@ -21,6 +21,7 @@ model User {
   name          String?
   image         String?
   emailVerified DateTime?
+  isAdmin       Boolean   @default(false)
 
   // Email/Password Authentication
   passwordHash String? // bcrypt hashed password for email auth users
@@ -347,6 +348,8 @@ model Activity {
   // Relations
   user           User      @relation(fields: [userId], references: [id], onDelete: Cascade)
   linkedWorkouts Workout[]
+  laps           Lap[]
+  splits         Split[]
 
   createdAt   DateTime      @default(now())
   updatedAt   DateTime      @updatedAt
@@ -357,6 +360,37 @@ model Activity {
   @@index([userId, type, startDate]) // For filtering by type within date range
   @@index([userId, hasHeartrate]) // For HR-based metrics queries
   @@index([userId, estimatedVdot]) // For VDOT trend analytics
+  @@index([stravaId])
+}
+
+model Lap {
+  id         String    @id @default(cuid())
+  activityId String
+  index      Int
+  startDate  DateTime
+  endDate    DateTime
+  distance   Float?
+  movingTime Int?
+  totalElevationGain Float?
+
+  activity Activity @relation(fields: [activityId], references: [id], onDelete: Cascade)
+
+  @@index([activityId])
+}
+
+model Split {
+  id         String    @id @default(cuid())
+  activityId String
+  index      Int
+  distance   Float?
+  movingTime Int?
+  averageSpeed Float?
+  averageHr    Float?
+  totalElevationGain Float?
+
+  activity Activity @relation(fields: [activityId], references: [id], onDelete: Cascade)
+
+  @@index([activityId])
 }
 
 enum ActivityType {

commit f21d942c8c961937fdc49e8d604d337397930760
Author: Thies <thies.schuelken@gmail.com>
Date:   Thu Feb 12 09:43:59 2026 +0200

    Add AiUsageHistory model and logic to fix build error

diff --git a/Web/prisma/schema.prisma b/Web/prisma/schema.prisma
index 33044f9..02658eb 100644
--- a/Web/prisma/schema.prisma
+++ b/Web/prisma/schema.prisma
@@ -82,6 +82,7 @@ model User {
   notifications Notification[]
   chatMessages  ChatMessage[]
   chatSessions  ChatSession[]
+  usageHistory  AiUsageHistory[]
 }
 
 // ============================================
@@ -599,3 +600,20 @@ model AiDailyTokenUsage {
   @@unique([date, providerId])
   @@index([date])
 }
+
+// Granular AI usage history per request
+model AiUsageHistory {
+  id           String   @id @default(cuid())
+  userId       String
+  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
+  
+  providerId   String?
+  model        String?
+  
+  inputTokens  Int      @default(0)
+  outputTokens Int      @default(0)
+  timestamp    DateTime @default(now())
+
+  @@index([userId])
+  @@index([timestamp])
+}

commit 733200632092b6928dbb96049555b3bd471bfcd9
Author: Thies <thies.schuelken@gmail.com>
Date:   Thu Feb 12 08:32:36 2026 +0200

    feat: implement ChatGPT-style layout with sessions and sidebar

diff --git a/Web/prisma/schema.prisma b/Web/prisma/schema.prisma
index 243d91c..33044f9 100644
--- a/Web/prisma/schema.prisma
+++ b/Web/prisma/schema.prisma
@@ -81,6 +81,7 @@ model User {
   DailyFitness  DailyFitness[]
   notifications Notification[]
   chatMessages  ChatMessage[]
+  chatSessions  ChatSession[]
 }
 
 // ============================================
@@ -561,9 +562,27 @@ model ChatMessage {
   activityId String?
   activity   Activity? @relation(fields: [activityId], references: [id])
 
+  // Link to ChatSession
+  sessionId String?
+  session   ChatSession? @relation(fields: [sessionId], references: [id], onDelete: Cascade)
+
   user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@index([userId, createdAt])
+  @@index([sessionId])
+}
+
+model ChatSession {
+  id        String   @id @default(cuid())
+  userId    String
+  title     String   @default("New Chat")
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  
+  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
+  messages  ChatMessage[]
+
+  @@index([userId, updatedAt])
 }
 
 // Daily aggregated token usage for analytics

commit 96e822741bdffd8e05da16032231032b502a842b
Author: Thies <thies.schuelken@gmail.com>
Date:   Wed Feb 11 23:41:21 2026 +0200

    feat: implement AI Analytics Board and fix chat history/layout issues

diff --git a/Web/prisma/schema.prisma b/Web/prisma/schema.prisma
index c1066cf..243d91c 100644
--- a/Web/prisma/schema.prisma
+++ b/Web/prisma/schema.prisma
@@ -151,6 +151,7 @@ model AiProvider {
   lastUsageReset          DateTime @default(now())
 
   globalSettings GlobalAiSettings[]
+  dailyUsage     AiDailyTokenUsage[]
 
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
@@ -564,3 +565,18 @@ model ChatMessage {
 
   @@index([userId, createdAt])
 }
+
+// Daily aggregated token usage for analytics
+model AiDailyTokenUsage {
+  id           String   @id @default(cuid())
+  date         DateTime @db.Date // Midnight UTC
+  
+  inputTokens  Int @default(0)
+  outputTokens Int @default(0)
+  
+  providerId   String
+  provider     AiProvider @relation(fields: [providerId], references: [id], onDelete: Cascade)
+
+  @@unique([date, providerId])
+  @@index([date])
+}
